# SlaytheSpire_C — Project Instructions

> **Language Rule (HARD REQUIREMENT):** All user-facing output, code comments, doc strings, and log messages MUST be written in **Traditional Chinese (繁體中文)**. This file itself is in English; everything you produce for the user/codebase is in Traditional Chinese.

---

## 0. Product & Audience

- One-line product: 復刻 Slay the Spire 核心循環(卡牌戰鬥 + 爬塔地圖 + 遺物/藥水)的學習專案,目標是做出可玩版本。
- Target platform: PC, mouse-first(拖拉出牌為主要互動)。Mobile 未列入目標。
- The user is a game developer & engineer; normal engineering vocabulary is fine.
- Every system stays designer-tunable in the editor: serialized/tunable fields + 繁中 tooltips over hardcoded values; data assets (ScriptableObject) for tuning; sensible defaults; setup >5 manual steps = smell.
- Reply shape for features: brief code summary → concrete setup/wiring steps (exact names) → risks.

## 1. Stack Overview

|Item|Value|
|---|---|
|Type|Game (Unity)|
|Engine|Unity **6000.4.3f1** (from `ProjectSettings/ProjectVersion.txt`)|
|Render pipeline|URP 17.4.0 (`Assets/Settings/PC_RPAsset.asset` is the PC pipeline asset)|
|Input|Input System 1.19.0 (`Assets/InputSystem_Actions.inputactions` — template file, NOT wired yet)|
|Target platform|PC (mouse-first)|
|Source root|`Assets/Scripts/` — `Core/` (pure logic) + `Tests/EditMode/`; glue/UI layers not created yet|
|Build command|unverified (no build has been run yet)|
|Test command|EditMode tests via TestRunnerApi — see `.claude/skills/verify/` (verified 2026-08-03, 9/9 green)|
|Version control|git, initialized 2026-08-03, standard Unity .gitignore|

## 2. Dependencies (use these — do NOT reinvent)

|Dependency|Where / Version|Role|
|---|---|---|
|URP|com.unity.render-pipelines.universal 17.4.0|Render pipeline. Settings live in `Assets/Settings/`|
|Input System|com.unity.inputsystem 1.19.0|Input. Template `.inputactions` exists but is not referenced by any code yet|
|uGUI|com.unity.ugui 2.0.0|**IN USE — the decided UI route.** All game UI is uGUI Canvas|
|DOTween Pro|`Assets/Plugins/Demigiant/` (version unverified)|**IN USE from M4** — all UI motion/tweens. Read global skill `dotween-pro` before writing tween code|
|Animancer|`Packages/com.kybernetik.animancer` (embedded)|**INSTALLED, NOT USED in vertical slice** — reserved for the art-integration phase; do not reference it yet|
|Unity Test Framework|com.unity.test-framework 1.6.0|**IN USE** by `STS.Core.Tests`|
|Newtonsoft JSON|com.unity.nuget.newtonsoft-json 3.2.1 (**explicitly pinned in manifest** 2026-08-03, user-approved)|**IN USE** by `STS.Data` (content pipeline JSON parsing)|
|AI Assistant|com.unity.ai.assistant 2.17.0-pre.1|**Do NOT remove.** [推論] Provides the `IRunCommand` dynamic-execution base that unity-mcp `Unity_RunCommand` runs on (verify skill depends on it)|
|Visual Scripting|com.unity.visualscripting 1.9.11|**INSTALLED BUT UNUSED — do NOT use.** All gameplay logic is written in C#|
|Timeline / Multiplayer Center / Collab Proxy|template defaults|**UNUSED** — template leftovers, do not build on them without asking|

**Not installed — do not suggest / do not reference without asking first:** Cinemachine, any other third-party plugin. Adding packages requires explicit user approval (global rule).

### 2.1 UI route — DECIDED: uGUI + DOTween (2026-08-03, user-confirmed)

- All game UI is uGUI (Canvas) + DOTween Pro. **UI Toolkit (UXML/USS) is banned in this project** — do not add either.
- Read global skills `dotween-pro` + `ui-motion-fx` BEFORE writing any UI/tween code (tween lifecycle/autoKill are known traps).
- EventSystem must use `InputSystemUIInputModule` (Input System rule); forgetting this makes uGUI drag silently dead.

### Usage Rules

- **Input:** all player input goes through Input System action maps (edit `InputSystem_Actions.inputactions` or replace it); never `Input.GetKey`/legacy input.
- **Rendering:** URP is active; do not add Built-in RP shaders/assets.
- **Gameplay logic:** C# only; Visual Scripting is banned.
- **MCP bridges (verified 2026-08-03):** `unity-mcp` tools work against this project. `coplay-mcp` bridge is NOT installed in this project — its tools fail with "Editor is not running at the specified project root"; do not waste time retrying it, use unity-mcp.

## 3. Code Style

- C#: PascalCase types/methods/properties; camelCase locals/parameters; `_camelCase` private fields is acceptable but stay consistent per file.
- Namespaces: pure logic under `STS.Core.*`; tests under `STS.Core.Tests`.
- Comments: Traditional Chinese, only for non-obvious *why* (see §7).
- Serialized fields: `[SerializeField]` + 繁中 `[Tooltip]` for every designer-facing value.

## 4. Architecture

- **Data/config (M3, LIVE):** **`Assets/Data/Source/*.json` is the SINGLE SOURCE OF TRUTH** (cards/enemies/relics/potions/encounters/balance). ScriptableObject assets under `Assets/Data/<category>/` are GENERATED by the importer (menu `STS/重新匯入資料(JSON→SO)`) — hand-edits to SOs get overwritten on reimport; change the JSON instead. Importer updates SOs in place (GUID-preserving) and validates everything BEFORE writing (bad data aborts untouched). Known behavior: removing a JSON entry orphans its old .asset (dropped from GameDatabase but left on disk) — delete manually via editor if it matters.
- **Logic:** pure C# in `Assets/Scripts/Core/` (assembly `STS.Core`, `noEngineReferences: true` — it cannot touch UnityEngine, keep it that way; this is what makes it unit-testable).
- **Data assembly:** `Assets/Scripts/Data/` (`STS.Data`, pure .NET + precompiled Newtonsoft) — `ContentParser` (strict schema: unknown JSON fields = error; op/field combo whitelist; cross-reference checks) → `ParsedContent` → `ContentDb : IContentDb`.
- **Glue:** `Assets/Scripts/Game/` (`STS.Game`, engine refs allowed) — 7 SO types in `DataAssets/` mirroring the defs with 繁中 tooltips; `GameDatabaseAsset.BuildDb()` is the runtime entry (engine still sees only `IContentDb`). Editor-only importer lives in `Assets/Scripts/Game/Editor/` (`STS.Game.Editor`).

### Current Systems Map (verified against code 2026-08-03 — refresh via /harness-audit)

- **Combat math:** `STS.Core.Combat.CombatMath` — attack damage (strength add → weak ×0.75 → vulnerable ×1.5, float-multiply then single floor) + block absorption; `BlockMath` — block gain (dexterity add → frail ×0.75, floor). Damage/block modifiers live ONLY in these two classes; hooks must not become a second damage pipeline.
- **Combat engine (M1+M2, 2026-08-03):** `STS.Core.Combat.CombatEngine` — command-in/event-out (UI consumes the `CombatEvent` queue, never queries back mid-playback; `GetIntentPreview` is the ONLY sanctioned query, damage recomputed live), `CombatPhase` turn state machine incl. `AwaitingChoice` (card interrupts via `RequestChoice`/`ResolveChoice`), piles (hand cap 10, empty-pile reshuffle), energy incl. X-cost (`CostIsX`: spend-all, `XEnergySpent` feeds XEnergy/RepeatIsX), potions (`UsePotion`, shared EffectResolver). Block clears at the START of the owner's turn (test-locked). `EffectResolver` resolves all `EffectStep[]` ops except `Custom` (throws — deliberate escape hatch for M3+). Three damage paths ONLY: attack (CombatMath, fires Attack hooks), non-attack (block yes, modifiers no, NO hooks — thorns/SharpHide, prevents retaliation loops), direct hp loss (`LoseHp`, ignores block).
- **Hooks/Statuses/Relics (M2):** `HookBus` fires in deterministic order: player relics (acquisition) → player statuses (application) → enemy statuses; snapshots before iterating. 13 statuses in `StatusRegistry` (behavior in code, stacks in data); decay: timed debuffs -1 at OWNER's turn end, `JustApplied` skip only when applied during owner's OWN turn ([近似] StS semantics, test-locked). 10 relics in `RelicRegistry` (`RelicInstance.Counter` persists across combats — caller owns the instances).
- **Enemies (M2):** `EnemyDef`/`MoveDef` (moves = same EffectStep pipeline), `OpeningScript` then Loop/Weighted (`MaxConsecutive` reroll)/Custom AI; `GuardianAi` = mode-switch boss (damage threshold → defensive seq + SharpHide, then back). Next intent chosen right after the enemy acts (EnemyAi rng stream).
- **RNG:** `STS.Core.Rng.RngStream` (SplitMix64; deliberately a class, not struct — copy semantics would silently fork streams) + `RunRng` named streams (Map/CardReward/PotionReward/RelicReward/Shuffle/EnemyAi/CombatMisc). Determinism is the testing backbone; never use System.Random.
- **Cards:** `STS.Core.Cards` — `CardDef` (upgrade = separate def, id convention `strike`/`strike+`), `CardInstance` (InstanceId for UI tracking), `EffectStep` (flat op schema, maps 1:1 to future JSON). `STS.Core.Content.IContentDb` is the only content lookup the engine sees.
- **Feel/Perf (M7, 2026-08-28):** hit-feel pass — enemy hit flash+punch (`EnemyView.PlayHitFeedback`), attack lunge, death shrink, player HUD shake, tuned beats; all tweens follow DOTween discipline (DOKill-then-tween, SetLink). Perf baseline recorded in verify skill 管道4 (avg 8.58ms / worst 38.47ms / GC ~2.6MB/s during auto-run combat; regression thresholds defined). **Bulk numeric calibration of [近似] values deliberately deferred: it happens by user-supplied data table → edit JSON → reimport, zero code.** Known deferred polish: targeting arrow is a straight line; need-target potions auto-pick first living enemy; hand rebuild (not pooled).
- **Run UI (M6, 2026-08-28):** full-loop screens in `STS.Game.UI` — MapScreenController (node graph + edges + reachable pulse + ScrollRect + run HUD), Reward/Shop/Rest/EndScreenController, DeckViewOverlay (view/pick modes, self-destroying). **Router+action-hub pattern: `GameController` owns ALL screen actions (`EnterNodeFromMap`/`RewardTakeCard`/`ShopLeave`/`RestHealAction`…) — screen buttons call these game-level methods, and the full-run smoke (`煙霧_啟動自動一輪`, writes `Temp/STS_RunSmoke.txt`) walks the SAME methods.** Combat hands back via `CombatScreenController.OnCombatEnded` callback; used potions synced to RunState by before/after diff; combat overlay widgets live under a self-destroyed container (no orphans on screen switch). **`Application.runInBackground = true` is set in Awake — without it an unfocused editor freezes play-mode frames and automated smoke silently hangs (paid-for lesson).**
- **UI (M4, 2026-08-27):** code-built uGUI — the scene (`Assets/Scenes/Main.unity`, generated by menu `STS/建置主場景`) holds ONLY GameRoot(`GameController`)+EventSystem(`InputSystemUIInputModule`)+camera; ALL widgets are constructed at runtime by `STS.Game.UI.*` via `UiKit` (placeholder art = colored `Image` + TMP text; deliberate for the pre-art phase — art phase will introduce prefabs). `CombatScreenController` is the ONLY class touching the engine: command in, then plays the `CombatEvent` queue with beats, input locked during playback (`InputEnabled`). `GameController` self-heals null refs from Resources (`Assets/Data/Resources/GameDatabase.asset`, `Assets/Fonts/Resources/NotoSansTC SDF.asset`) — scene wiring is optional. Smoke hook: `CombatScreenController.煙霧_出第一張可出的牌()`.
- **Fonts/TMP:** TMP Essentials imported (`Assets/TextMesh Pro/`); 繁中 font = Noto Sans TC (OFL, `Assets/Fonts/NotoSansTC-Regular.otf`) with dynamic TMP asset generated by menu `STS/生成 TMP 繁中字型資產`. `UiKit.MainFont` is injected by GameController at Awake.
- **DOTween asmdef fact:** DOTween UI-module shortcuts (`DOAnchorPos`/`CanvasGroup.DOFade`…) live in loose Module sources — an asmdef-isolated assembly CANNOT see them until `Assets/Plugins/Demigiant/DOTween/Modules/DOTween.Modules.asmdef` exists (generated via DOTween Utility Panel → Create ASMDEF, user-clicked 2026-08-27); `STS.Game` references `DOTween.Modules`. Do not delete that asmdef.
- **Map/Run (M5, 2026-08-27):** `STS.Core.Map.MapGenerator` — grid paths bottom-up (±1 col, merge-on-collision, NO crossing — all structural params in BalanceDef), fixed rows (0=Combat, mid=Treasure, last=Rest, virtual Boss node), weighted types with min-row constraints; Map rng stream only, seed-locked by tests. `STS.Core.Run.RunEngine` — `NewRun` (starting loadout from BalanceDef), `EnterNode` (rolls contents AT entry), encounter pools (weak first N normal fights), `ApplyCombatResult` (rewards: gold/card-3-pick/potion drop with ±delta floating chance/elite relic), shop (buy card/relic/potion, remove service with escalating cost), rest (heal %/upgrade). Combat itself stays in CombatEngine; Run only assembles `CombatSetup` and writes back results. `IContentCatalog` (extends IContentDb) is the Run layer's full-catalog view — engine still sees only IContentDb.
- **Content (M3):** 32 cards (26 deck-buildable + upgrades + 3 status cards), 9 enemies (incl. guardian boss whose move ids MUST match `GuardianAi` constants — test-locked), 10 relics (data ids MUST align with `RelicIds.All` — parser-enforced), 3 potions, 9 encounters across Weak/Normal/Elite/Boss pools, balance table. Plus `statuses.json` (13 status names/descriptions for tooltips). **All numbers are [近似] reconstructions — calibration (M7) means editing JSON, never code.**
- **Potions ignore modifiers (2026-08-28, user-specified rule):** `EffectResolver.Resolve(..., ignoreModifiers)` — potions route damage through `DealNonAttackDamage` and block through `GainBlockRaw`, so Strength/Weak/Vulnerable/Dexterity/Frail never touch them (block absorption still applies). `CombatEngine.UsePotion` passes true; the flag survives an `AwaitingChoice` interrupt via `_pendingIgnoreModifiers`.
- **Hand is incremental (2026-08-28):** `HandView` keys `CardView`s by `CardInstance.InstanceId` — new cards fly in from the draw pile, played/discarded cards animate out and self-destroy, survivors just tween to their new slot. Never go back to destroy-and-rebuild: it kills every per-card animation.
- **Choice mode (2026-08-28):** exhaust-a-card effects are picked by CLICKING the real hand cards (`CombatScreenController.IsChoiceMode` + `CardView.OnPointerClick`), auto-submitting once the required count is selected; dragging is blocked while choosing. There is no choice panel — `ChoicePanelView` was deleted.
- **Combat layout (2026-08-28, matches the user's reference screenshot):** top info bar (HP / gold / 3 potion slots / floor / deck button) → `TopBarView`; relic chips just below it; **player figure on the LEFT, enemies on the RIGHT** (HP bars directly under each); energy orb bottom-left (`EnergyOrbView`); draw pile bottom-left corner, discard+exhaust bottom-right; end-turn bottom-right; hand fanned bottom-center.
- **Targeted card play (2026-08-28):** a card that needs a target does NOT follow the cursor — it locks to `瞄準位置` (0, 250) in hand space and a curved segmented arrow (`TargetArrowView`, quadratic bezier + head) runs from the card to the cursor. Non-target cards still drag normally. Arrow origin uses `Rect.TransformPoint` — never add canvas-space offsets to screen-space positions.
- **Tooltips (2026-08-28):** `TooltipView` is created ONCE by `GameController` in the overlay layer (`game.Tooltip`) so every screen — combat, shop, reward, map — shares it + `TooltipTrigger` (hover component; content is a **delegate**, re-evaluated per hover because stacks/HP change) + `TooltipText` (composes engine state × data text). Hover targets: enemy body (HP/intent detail/every status with its full description), player HUD panel (HP/block/energy/statuses), relic chips in the new `RelicBarView`, potion buttons. **Status text lives in data**: `Assets/Data/Source/statuses.json` → `StatusDef` (`{n}` placeholder = live stack count); the parser REQUIRES every `StatusId` except None to have an entry, so adding a status to code without text fails the import.
- **Card damage preview (fixed 2026-08-28):** `CardTextFormatter.FormatDescription(def, player, target)` — `{dmg}` must run the SAME `CombatMath` path as the engine, INCLUDING the target's Vulnerable, or the card lies to the player (this exact bug shipped in M4-M6: mechanic worked, card text always showed base damage, and it read as "易傷沒有作用"). UI supplies the target: `HandView.DefaultPreviewTarget` = the lone living enemy (single-enemy fights), else null; while dragging, `CombatScreenController.UpdateTargeting` re-renders against the raycast-hit enemy (only when the hit index CHANGES — never per-frame TMP rebuilds).
- **Reflect/deferred-block rules (2026-08-28, user-reported logic bugs):** **SharpHide reflects PER HIT** — it hooks `HookPoint.AttackDealt` (not `CardPlayed`), so a 3-hit card takes 3 reflects. **Curl's block is DEFERRED to after the whole card/potion/enemy move** — `StatusRegistry` removes the stacks immediately and calls `CombatEngine.DeferBlock`; `FlushDeferredBlocks()` runs at the start of `FinishCardPlay`, after `UsePotion`'s Resolve, after each enemy move, and after each turn-end-in-hand step (dead units skipped). Applying the block instantly would let a multi-hit attack's later hits be absorbed by block its own first hit created. **Ordering invariant in `DealAttackDamage`: Attack hooks fire BEFORE `AfterHpLoss`** — otherwise the very hit that flips the Guardian into defensive mode gets reflected by the SharpHide it just created (this exact regression cost a test).
- **Rest screen + upgrade preview (2026-08-28, per user reference images):** `RestScreenController` = title 「我該做什麼呢?」 + two large option cards (休息 / 鍛造) + placeholder campfire with a pulsing tween. `DeckViewOverlay.Open(..., upgradedLookup:)` switches the picker into preview mode: clicking a card shows current ➜ upgraded side by side with 確認升級 / 再看看 before committing; `GameController.RestOpenUpgradePicker` filters out cards with no `+` def. **Placeholder symbols must be CJK or ASCII only** — Noto Sans TC has no glyph for ✦/➤ and they render as tofu boxes (both shipped once and were caught by screenshot).
- **Shop & rewards rework (2026-08-28, per user reference screenshots):** the shop node now opens `ShopRoomScreenController` (merchant room: hero left, merchant on a rug right, `前進` arrow leaves, clicking the merchant opens the counter) → `ShopScreenController` (green cloth: 5 class cards top, 2 colorless cards bottom-left, 3 relics over 3 potions centre-bottom, round remove-card service bottom-right, `< 返回` back to the room). **Shelf sizes are BalanceDef data** (`ShopClassCardCount/ShopColorlessCardCount/ShopRelicCount/ShopPotionCount`); relics and potions de-duplicate by re-drawing, never by dropping the slot (a dropped slot silently shrinks the shelf). `ShopInventory.CardIds` holds class cards then colorless ones, split by `ClassCardCount` — one buy path, not two.
- **Colorless cards (2026-08-28):** `CardDef.Colorless` (JSON `"colorless": true`) — excluded from the post-combat reward pool, drawn only for the shop's colorless shelf. Three exist: 鋼鐵閃光 / 巧勁 / 包紮.
- **Rewards are claimed item-by-item (2026-08-28):** `CombatRewards` carries `GoldClaimed/PotionClaimed/RelicClaimed/CardResolved`; `GenerateRewards` no longer credits anything. `ClaimRewardGold/Potion/Relic` credit on demand, `TakeCardReward`/`SkipCardReward` only close the card item, and `LeaveRewards()` is what returns to the map. `RewardScreenController` renders the 「搜刮!」 list and switches to a 「選擇一張牌」 card-pick mode in the same screen root.
- **Ironclad card pool = StS2 (2026-08-28, batch 1 of N):** the old StS1-based pool was replaced with the StS2 Ironclad set. **The blueprint moved from StS1 to StS2 for cards** (CLAUDE.md §0's StS1 note now applies only to systems we haven't re-sourced). Numbers/costs come from a community StS2 card database; all card text is written fresh in 繁中 in the project's own `{dmg}`/`{blk}` template — never pasted from the source. 49 playable ids live today (starters + Common/Uncommon/Rare) + 3 colorless + 3 status cards. Cards not yet expressible are deliberately absent, NOT stubbed — the remaining ~39 need: pile manipulation ops (play/move/upgrade cards across piles), a card-cost-modification system (next attack free, skills cost 0, cost scales with attacks played), per-instance mutable card state (damage that grows during combat), and card transformation. Two co-op cards (肉盾/惡魔護盾) are out of scope for a single-player slice.
- **Engine additions for the card pool (2026-08-28):** 9 statuses (`Barricade` skips block clearing entirely; `FeelNoPain`/`DarkEmbrace` ride the new `CardExhausted` hook; `Juggernaut` rides the new `BlockGained` hook; `Rupture`/`Inferno` ride `HpLost`; `Pyre`/`CrimsonMantle` fire at turn start; `Rage` decays at turn end). Two ops: `ExhaustTopOfDraw`, `DoubleStatus`. Four scaling `AmountKind`s — `PerExhaustedCard`/`PerTargetVulnerable`/`PerAttackPlayedThisTurn`/`PerStrikeCard` — all sharing one formula, `Amount + SecondaryAmount × count` (the parser rejects them without a `secondaryAmount`). **`HookPoint.HpLost` existed in the enum but was never fired by anything until now** — it is fired from `AfterHpLoss` for player and enemies alike, before the enemy-only death/mode-switch logic. **All card exhausts must go through `CombatEngine.ExhaustCard`** — there were 5 inline exhaust sites and any missed one silently skips the exhaust hooks. `CountCardsNamedContaining` must also count the card currently resolving: a played card has already left the hand and not yet reached the discard pile (this cost a test).
- **Pile-manipulation ops (2026-08-29, batch 2):** 5 more ops — `ExhaustHand`, `ExhaustNonAttacksInHand`, `DiscardHandDrawSame`, `DrawUntilNonAttack` (bounded at 20 iterations so an all-attack deck can't hang) — plus `AmountKind.PerLastExhausted`, which reads `CombatState.LastExhaustedCount` so a card can exhaust N cards in one step and convert N into block/damage in the next. **`ExhaustHand` lifts the whole selection out of the hand before exhausting any of it**: the exhaust hooks draw new cards (Dark Embrace), and mutating the hand while walking it would exhaust those fresh cards too. Unlocks 劫掠 / 重振精神 / 戰鼓 / 惡魔之焰 / 添柴 (54 playable ids now). Auto-play ops (`PlayTopOfDraw` for 破滅/傾瀉/彼岸咆哮) are deliberately NOT in this batch — they need a re-entrancy guard and a rule for what an auto-played card does with a choice interrupt.
- **Card preview must run the engine's own maths (2026-08-29, second time this bit us):** `CardTextFormatter` takes an optional `CombatEngine`; scaling counts come from `CombatEngine.ScalingCount`, the exact method the resolver calls. The first version of the scaling cards duplicated nothing — it simply ignored the new `AmountKind`s and previewed the base number, so 完美打擊 always read 6 while dealing far more. **Tests now assert "number shown on the card == HP the enemy actually lost" for every scaling card**; that assertion, not the per-card damage numbers, is what stops this recurring. `AttacksPlayedThisTurn` increments in `FinishCardPlay` (not on play) so preview and resolution see the same count.
- **Auto-play (2026-08-29, batch 3):** `EffectOp.PlayTopOfDraw` plays cards straight off the draw pile with no energy cost and no player targeting (target = a random living enemy); its `Pile` field says where the card lands afterwards (`Exhaust` for 破滅, `Discard` for 傾瀉), and Powers still go to `PowersPlayed`. On an X-cost card the step's `amount` is an *extra* count on top of X (傾瀉+ = X+1). Three invariants worth keeping: **`_autoPlayDepth` caps nesting at 4** (破滅 flipping into 破滅 recurses otherwise); **`EffectResolver` takes an `autoPlay` flag that turns choice steps into random exhausts** — `RequestChoice` would park the game in `AwaitingChoice` while the auto-play loop is still mid-flight, stranding the remaining cards; and **Unplayable cards are moved, never resolved**. 彼岸咆哮 is modelled as a status that re-deals its AoE at turn start via `DamageAllEnemiesAttack` (real attack damage, so Strength/Vulnerable/thorns all apply) rather than literally replaying a card out of the exhaust pile. 57 playable ids now.
- **Cost modification (2026-08-29, batch 4):** **`CombatEngine.GetCardCost(def)` is the single source of a card's current cost** — `CanPlayCard`, the actual charge in `PlayCard`, and the number printed on the card face all call it (a discounted card also prints its cost in green). Three modifiers: `NextAttackFree` (無情猛攻 — consumed whenever an Attack is played, whether or not it saved energy), `Corruption` (腐化 — Skills cost 0 AND are force-exhausted in `FinishCardPlay`, separate from `def.Exhausts`), and per-card `CardDef.CostScaling` (踩踏 — `MinusPerAttackPlayedThisTurn`, floored at 0). `NextAttackDoubled` (連環拳) re-resolves the card's steps once in `FinishCardPlay` — that is the one convergence point both the normal and the choice-interrupted play paths reach — using `autoPlay: true`, since the repeat can't stop for a second player choice. 61 playable ids now.
- **Per-instance card growth (2026-08-29, batch 5):** `CombatState.CardDamageBonus` is a `Dictionary<InstanceId,int>` of damage a specific card has accumulated **this combat**. It lives on `CombatState`, never on `CardInstance` — `CardInstance` is run-scoped data shared across combats, so writing the bonus there would carry it into the next fight. `ResolveDamageBase` adds `engine.CurrentCardDamageBonus` (the bonus of the card currently resolving) on top of every `AmountKind`. Ops: `GrowThisCardDamage` (暴走) and `AbsorbRandomAttackFromHand` (痛毆 — exhausts a random Attack from hand and folds its base damage in). `CombatState.HpLossEventsThisCombat` counts HP-loss *events*, not points (扯碎).
- **`RepeatIsX` became `RepeatKind` (2026-08-29):** the bool grew a third case (`Fixed` / `XEnergy` / `PerHpLossThisCombat`), so it is now an enum and `EffectResolver.ResolveRepeat` is the only place that reads it. JSON key is `repeatKind` (string enum); the old `repeatIsX` boolean no longer exists anywhere. The parser still enforces "X-typed fields only on `costIsX` cards".
- **Tests:** 148 tests, all green (2026-08-29), TWO assemblies: `STS.Core.Tests` 117 (engine+formatter+MapGenerator; `Assets/Scripts/Tests/EditMode/`, shared fixtures in `TestContent.cs`) + `STS.Content.Tests` 31 (content gate + RunEngine on real data incl. a full auto-crawl integration with real combats). **Run BOTH: TestRunner filter assemblyNames = ["STS.Core.Tests", "STS.Content.Tests"].** Shop-shelf tests loop several seeds on purpose — a single seed let a "duplicate draw silently shrinks the shelf" bug pass.
- **Assemblies:** `STS.Core` (pure) / `STS.Data` (pure + Newtonsoft) / `STS.Game` (engine refs) / `STS.Game.Editor` (importer) / `STS.Core.Tests` / `STS.Content.Tests`.
- **Scene:** `Assets/Scenes/Main.unity` — the game scene (regenerable via menu). `SampleScene.unity` is template debris; ignore.
- **Known stale paths:** `Assets/InputSystem_Actions.inputactions` is a template action map wired to nothing — treat contents as placeholder, not design.
- **Known debris:** `Assets/TutorialInfo/` + `Assets/Readme.asset` — URP template leftovers; ignore, do not "fix"/delete during unrelated work.

## 5. Performance / Hot Paths

- Zero allocations in per-frame paths (global rule). Card game hot spots to watch later: hand layout re-calc, hover/drag per-frame logic, damage-number spawning (pool them).
- No perf budget measured yet — establish one when a real combat scene exists.

## 6. Logging & Error Handling

- Use `Debug.Log/LogWarning/LogError` with 繁中 messages; no custom logger exists.
- No empty catch blocks; let unexpected exceptions surface — the verify smoke step scans for them.

## 7. Comments & Documentation

- Default to no comments; names explain *what*. Comment only non-obvious *why* (invariants, StS rule references, workarounds), in Traditional Chinese, one short line.

## 8. Working Protocol for the AI

1. **Plan first (in Traditional Chinese)** — approach, which dependencies apply, which files change.
2. **Check §2 triggers** — Input System for input, URP for rendering, C# only for logic; UI work requires settling §2.1 first.
3. **Confirm before large refactors.** Single-file edits and additive features proceed directly.
4. **Finish the job.** No TODO, no stubs.
5. **If unsure, say so.** Do not fabricate API signatures — verify against installed source or `unity_reflect`.
6. **Verify before "done".** Follow `.claude/skills/verify/`: compile check is mandatory after any code change; EditMode tests after logic changes; play-mode smoke + log scan after behavior/scene changes; report evidence tiers ([驗證]/[推論]/[假設]); never report completion without evidence.
7. **Pure logic goes to STS.Core with tests** — copying the CombatMath + CombatMathTests pattern is the default for rules/calculations/state machines.
